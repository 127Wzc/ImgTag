# PostgreSQL 连接字符串（必须配置）
PG_CONNECTION_STRING=postgresql://user:password@localhost:5432/imgtag

# 服务地址（可选，默认 http://localhost:8000）
# BASE_URL=http://localhost:8000

# 上传目录（可选，默认 ./uploads）
# UPLOAD_DIR=/path/to/uploads
